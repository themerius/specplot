[project]
name = "specplot"
version = "0.1.0"
description = "Architecture diagramming tool with Python DSL"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "drawsvg>=2.0",
    "httpx>=0.27",
    "networkx>=3.6.1"
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]
